# 🚀 ClashFa Wizard Release Notes

- Rebranded project from BPB Wizard to ClashFa Wizard.
- Added worker source selection for deploy/update with support for custom raw URLs.
- Kept Cloudflare upload filename fixed as `worker.js` regardless of source filename/extension.
- Simplified deployment requirements by removing legacy per-worker secrets/env variables.
- Improved compatibility for forked repository links and installer endpoints.
